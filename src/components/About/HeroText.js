import { HeroTextStyles } from "./HeroTextStyles";

const HeroText = () => {
  return (
    <HeroTextStyles>
      <p class="line-1 anim-typewriter">Full-Stack Software Engineer</p>
    </HeroTextStyles>
  );
};

export default HeroText;
